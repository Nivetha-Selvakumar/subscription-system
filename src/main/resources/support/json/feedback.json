{
  "feedbackCreate": {
    "body": [
      {
        "name": "ratings",
        "required": true,
        "maxLength": 1,
        "format": "^[0-5]$",
        "displayName": "Ratings"
      },
      {
        "name": "comments",
        "required": true,
        "maxLength": 255,
        "format": "^[A-Za-z0-9 ]+$",
        "displayName": "Comments"
      }
    ]
  },
  "feedbackEdit": {
    "body": [
      {
        "name": "ratings",
        "required": true,
        "maxLength": 1,
        "format": "^[0-5]$",
        "displayName": "Ratings"
      },
      {
        "name": "comments",
        "required": true,
        "maxLength": 255,
        "format": "^[A-Za-z0-9 ]+$",
        "displayName": "Comments"
      },
      {
        "name": "status",
        "required": false,
        "maxLength": 30,
        "format": "^[A-Za-z]+$",
        "displayName": "Status"
      }
    ]
  }
}